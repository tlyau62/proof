# Problem

Can we create a machine H which it can determine a given machine M and a given input I will halt or not.

For example, if the machine M with input I will halt, then H(M, I) will return true. Similarily, if the machine M with input I' will not half, then H(M, I') will return false.

